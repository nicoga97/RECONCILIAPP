<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bsocial.reconciliapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Reconciliapp">
        <activity android:name=".WorkMatchActivity"></activity>
        <activity android:name=".UniversityMatchActivity" />
        <activity android:name=".StoryNavigationActivity" />
        <activity android:name=".StoryActivity" />
        <activity android:name=".StoryBankActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".AlliesActivity" />
        <activity android:name=".AttentionActivity" />
        <activity android:name=".FirstMainPage" />
        <activity android:name=".Encuentros" />
        <activity android:name=".MainPage" />
        <activity android:name=".Conoce" />
        <activity android:name=".WelcomePage" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>